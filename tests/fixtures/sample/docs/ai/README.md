# Sample Project — AI Context

Fixture docs index. The entry point reachability is measured from.

## Modules

| Module | What it does | Docs |
|--------|--------------|------|
| [module](./module/) | The one documented module | Core |

> Note: the orphan doc in this folder is intentionally NOT linked from here —
> it exists so the orphan check has something to find. (Its name is not written
> as a backtick path on purpose: a backtick .md path IS a navigation edge.)
