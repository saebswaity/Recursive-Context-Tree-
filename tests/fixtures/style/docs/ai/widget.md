# Widget

> Cites the widget with a WRONG path style. The real file is `src/widget.py`,
> but this doc cites it module-relative, so verify must flag it AND suggest the
> correct repo-root-relative path.

## Key Files

| File | Purpose |
|------|---------|
| `lib/widget.py` | Wrong path — no such file. Real one is src/widget.py (basename matches). |
